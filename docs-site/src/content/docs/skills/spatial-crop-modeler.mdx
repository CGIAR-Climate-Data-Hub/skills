---
title: Spatial Crop Modeler
description: Runs DSSAT pixel-by-pixel across climate and soil datacubes to produce spatially-explicit yield maps for maize, wheat, and bean.
sidebar:
  order: 5
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs>
<TabItem label="Explanation">

**Type:** Crop modeling  
**Skill file:** `.agents/skills/spatial-crop-modeler/SKILL.md`  
**Raw URL:** `https://raw.githubusercontent.com/CGIAR-Climate-Data-Hub/skills/main/.agents/skills/spatial-crop-modeler/SKILL.md`

---

Orchestrates a full spatially-explicit crop yield simulation. You describe the area, crop,
and planting date. The skill checks your environment, generates a validated YAML config,
and runs DSSAT across every valid pixel in the datacube using `ag-cube-cm`.

## Two modes

| Mode | When to use | What happens |
|------|-------------|--------------|
| `with_cubes` | Weather and soil NetCDF files already on disk | Skip download — go straight to DSSAT |
| `full_pipeline` | Starting from scratch | Download CHIRPS + CHIRTS + AgERA5/NASA POWER + SoilGrids, build datacubes, then simulate |

## Climate sources

| Variable | Source | Notes |
|----------|--------|-------|
| Precipitation | CHIRPS | 0.05 deg, 1981–present, no API key |
| Max/Min temperature | CHIRTS | 0.05 deg, no API key |
| Solar radiation | AgERA5 | 0.1 deg — CDS key required at `~/.cdsapirc` |
| Solar radiation | NASA POWER | 0.5 deg — no API key, use when CDS unavailable |

## Supported crops and cultivars

| Cultivar | Crop | Best for |
|----------|------|----------|
| `IB1072` | Maize | Tropical / Central America / Sub-Saharan Africa |
| `PC0002` | Maize | Temperate regions |
| `IB1015` | Spring Wheat | Ethiopia, South Asia |
| `IB1487` | Winter Wheat | Europe, North Africa |
| `IB0001` | Bean / Soybean | General |

## Output

A NetCDF file with variable `HWAM` (harvest grain yield, kg/ha) and dimensions:

```
planting_window × year × y (latitude) × x (longitude)
```

After every run the skill reports:

```
pixels_ok      : 7   — DSSAT ran successfully
pixels_skipped : 18  — NaN weather or soil (expected at grid edges)
pixels_failed  : 0   — DSSAT runtime errors (investigate if > 5%)
mean HWAM      : 8782 kg/ha
```

Skipped pixels are normal — they occur where the coarser AgERA5 (0.1 deg) or SoilGrids
grids do not fully cover the finer CHIRPS (0.05 deg) grid edges.

## Install

```bash
pip install "ag-cube-cm[models] @ git+https://github.com/anaguilarar/ag-cube-cm.git"
pip install "aggeodata[download,mcp] @ git+https://github.com/anaguilarar/aggeodata.git"
```

:::caution[working_path must have no spaces]
DSSAT is a Fortran program that silently fails on paths with spaces.
Use `D:/dssat_work`, not `D:/OneDrive - CGIAR/...` for `working_path`.
Output NetCDF files can go anywhere.
:::

## AgERA5 setup (CDS key)

Required only when using AgERA5 for solar radiation. Register free at
[cds.climate.copernicus.eu](https://cds.climate.copernicus.eu/), then create `~/.cdsapirc`:

```
url: https://cds.climate.copernicus.eu/api/v2
key: <YOUR-UID>:<YOUR-API-KEY>
```

Use `rsds: nasa_power` in the config to skip this requirement entirely.

## Config schema reference

**with_cubes**

```yaml
mode: with_cubes

weather_path: D:/cubes/climate_hnd_2021_2021.nc
soil_path:    D:/cubes/soil_hnd.nc
output_path:  D:/results/yield_hnd.nc

simulation:
  country:      Honduras
  country_code: HND
  model:        dssat
  working_path: D:/dssat_work   # no spaces
  ncores:       2
  max_pixels:   null

crop:
  name:     Maize
  cultivar: IB1072

management:
  planting_date:        "2021-05-15"
  n_planting_windows:   1
  planting_window_days: 14
  fertilizer_schedule:  []
```

**full_pipeline**

```yaml
mode: full_pipeline

output_dir: D:/ag_cube_test/hnd_small
suffix:     hnd_small

spatial:
  bbox: [-87.5, 14.2, -87.2, 14.5]   # [xmin, ymin, xmax, ymax] WGS-84

dates:
  start: "2021-01-01"
  end:   "2021-12-31"

climate:
  sources:
    pr:     chirps
    tasmax: chirts
    tasmin: chirts
    rsds:   nasa_power   # or agera5 if CDS key available
  ncores:             2
  agera5_version:     "2_0"
  reference_variable: pr

soil:
  variables: [clay, sand, silt, bdod, cfvo, soc, phh2o, wv0010, wv0033, wv1500]
  depths:    ["0-5", "5-15", "15-30", "30-60", "60-100"]
  reference_variable: wv1500

simulation:
  country:      Honduras
  country_code: HND
  model:        dssat
  working_path: D:/dssat_work
  ncores:       2
  max_pixels:   null

crop:
  name:     Maize
  cultivar: IB1072

management:
  planting_date:        "2021-05-15"
  n_planting_windows:   1
  planting_window_days: 14
  fertilizer_schedule:  []
```

</TabItem>
<TabItem label="Claude Code">

Complete the [Claude Code setup](../usage/claude-code/) first, then launch `claude` from the project root.

**Example request — with existing datacubes**

```
I want to run DSSAT for maize in Honduras. I already have the weather cube at
D:/ag_cube_test/hnd_small/climate_raw/climate_hnd_small_2021_2021.nc and the soil
cube at D:/ag_cube_test/hnd_small/soil_hnd_small.nc. Output to D:/results/yield_hnd.nc.
Use cultivar IB1072 with planting date May 15 2021.
```

**Example request — full pipeline from scratch**

```
Simulate maize yield for a small area in Honduras, bbox [-87.5, 14.2, -87.2, 14.5],
year 2021. Download climate from CHIRPS, CHIRTS, and NASA POWER (no CDS key).
Output everything to D:/ag_cube_test/hnd_small.
```

**Worked example result (Honduras, Maize IB1072, 2021)**

```
pixels_ok      : 7
pixels_skipped : 18
pixels_failed  : 0
mean HWAM      : 8782 kg/ha  (range: 8145–9787)
```

**Validating and running manually**

```bash
ag-cube-cm validate config.yaml
ag-cube-cm run config.yaml
```

</TabItem>
<TabItem label="Antigravity">

Complete the [Antigravity setup](../usage/antigravity/) first, then paste the system prompt below.
No manual install needed — the prompt installs `ag-cube-cm` and `aggeodata` inside the sandbox.

**System prompt**

```
== ENVIRONMENT SETUP (run once before anything else) ==
Run the following command and wait for it to complete before proceeding:

pip install "ag-cube-cm[models] @ git+https://github.com/anaguilarar/ag-cube-cm.git" "aggeodata[download,mcp] @ git+https://github.com/anaguilarar/aggeodata.git" s3fs zarr nbformat plotly

== SKILL LOADING ==
When the user gives you a spatial crop modeling task (DSSAT, yield simulation, ag-cube-cm,
"simulate maize/wheat/bean yield over [region]"), use the CDH skills from GitHub.

Before planning or executing anything, use read_url_content to fetch ALL five skill files now:

1. https://raw.githubusercontent.com/CGIAR-Climate-Data-Hub/skills/main/.agents/skills/spatial-crop-modeler/SKILL.md
2. https://raw.githubusercontent.com/CGIAR-Climate-Data-Hub/skills/main/.agents/skills/climate-data-download/SKILL.md
3. https://raw.githubusercontent.com/CGIAR-Climate-Data-Hub/skills/main/.agents/skills/soil-data-download/SKILL.md
4. https://raw.githubusercontent.com/CGIAR-Climate-Data-Hub/skills/main/.agents/skills/geospatial-cube-processor/SKILL.md
5. https://raw.githubusercontent.com/CGIAR-Climate-Data-Hub/skills/main/.agents/skills/climate-dashboard/SKILL.md

Fetch all five BEFORE reading any of them. Treat spatial-crop-modeler as the controlling
workflow. The other four are delegated sub-skills:
  - climate-data-download → climate source routing (CHIRPS / CHIRTS / AgERA5 / NASA POWER / GEE)
                            and the critical ncores rule
  - soil-data-download → SoilGrids download, soil cube build (IGH→EPSG:4326), and cube validation
  - geospatial-cube-processor → summarize climate + yield NetCDFs into CSV per admin unit
  - climate-dashboard → build the final HTML dashboard with KPIs + yield map + climate charts

== EXECUTION RULES ==
- working_path in the ag-cube-cm config MUST have NO spaces (DSSAT is Fortran — silently fails).
  Use D:/dssat_work, never D:/OneDrive - CGIAR/... for working_path. Output NetCDFs can go anywhere.
- If the user selects GEE for ANY climate variable, force climate.ncores=1 in the YAML.
  GEE writes per-day GeoTIFFs through HDF5 without parallel I/O — ncores>1 crashes the model.
- After the simulation completes (HWAM NetCDF produced), FIRST run Step 7 quality
  gate from spatial-crop-modeler/SKILL.md. The gate computes ok / skipped / failed
  fractions and mean HWAM and raises a RuntimeError if any threshold fails.
  If it raises: STOP. Report the gate output verbatim to the user, inspect the
  spatial pattern of skipped/failed pixels, propose the concrete fix (water-body
  bbox, wrong planting season, grid mismatch, etc.), and wait for confirmation.
  Do NOT proceed to dashboards or notebooks until the gate passes or the user
  explicitly overrides it.
- ONLY after the gate passes, build the dashboard:
    a. Use geospatial-cube-processor to mask + summarize the climate cube to a CSV
       (per admin level 1 or full country, depending on bbox size).
    b. Use climate-dashboard on that CSV to produce the climate charts + KPI cards.
    c. Add a yield map panel from the HWAM NetCDF — mean over planting_window × year,
       rendered as a single PNG or embedded heatmap in the same HTML file.
- Do NOT re-fetch sub-skill files mid-task — they are already loaded.
- Do NOT inspect the aggeodata or ag-cube-cm packages with inspect() or dir() — all API
  patterns are in the skill files.
- Execute all generated code using run_command.
```

**Example request — full pipeline from scratch (Malawi maize)**

```
Simulate maize yield for Malawi for the 2021–2022 growing season.
Download climate from GEE (CHIRPS for precipitation, CHIRTS for Tmax/Tmin, AgERA5 for rsds).
Cultivar IB1072, planting date 2021-11-15, one planting window.
Output everything (datacubes, yield NetCDF, dashboard HTML) to D:/data/malawi.
```

Antigravity will fetch all four skill files, show a single plan covering download + simulation
+ dashboard, and after confirmation produce:

```
D:/data/malawi/
  climate_raw/                  — CHIRPS / CHIRTS / AgERA5 GEE downloads
  climate_malawi_2021_2022.nc   — stacked climate cube
  soil_malawi.nc                — SoilGrids cube
  yield_malawi_2021.nc          — DSSAT output (variable: HWAM, kg/ha)
  summary_ME_MWI_2021_2022.csv  — monthly climate per admin unit
  malawi_yield_climate_2021.html — dashboard (yield map + climate KPIs + filters)
```

**Example request — with existing datacubes**

```
I want to run DSSAT for maize in Honduras. I already have the weather cube at
D:/ag_cube_test/hnd_small/climate_raw/climate_hnd_small_2021_2021.nc and the soil
cube at D:/ag_cube_test/hnd_small/soil_hnd_small.nc. Output to D:/results/yield_hnd.nc.
Use cultivar IB1072 with planting date May 15 2021. Build a dashboard at the end.
```

**Troubleshooting**

| Symptom | Fix |
|---------|-----|
| `read_url_content` returns an error | Verify Antigravity has web access enabled |
| `ModuleNotFoundError: ag_cube_cm` | Re-run the pip install line in the system prompt |
| `DSSAT path not found` | Install DSSAT separately or set `dssat_path` in the YAML |
| Model crashes when using GEE source | Confirm `climate.ncores: 1` (HDF5 not parallel-safe) |
| `working_path` errors | Path contains spaces — use `D:/dssat_work` |
| Dashboard step skipped | Confirm climate-dashboard was in the fetched skill list |

</TabItem>
<TabItem label="Codex">

Complete the [OpenAI Codex setup](../usage/codex/) first, including the one-time system prompt.

**Example request — with existing datacubes**

```
I want to run DSSAT for maize in Honduras. I already have the weather cube at
D:/ag_cube_test/hnd_small/climate_raw/climate_hnd_small_2021_2021.nc and the soil
cube at D:/ag_cube_test/hnd_small/soil_hnd_small.nc. Output to D:/results/yield_hnd.nc.
Use cultivar IB1072 with planting date May 15 2021.
```

**Example request — full pipeline from scratch**

```
Simulate maize yield for a small area in Honduras, bbox [-87.5, 14.2, -87.2, 14.5],
year 2021. Download climate from CHIRPS, CHIRTS, and NASA POWER (no CDS key).
Output everything to D:/ag_cube_test/hnd_small.
```

</TabItem>
</Tabs>
